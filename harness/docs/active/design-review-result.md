# 设计审查报告 — 上下文层设计(2026-06-11)

> 审查对象:`docs/superpowers/specs/2026-06-10-context-layer-design.md`(819 行,自检三修后版本)
> 流程:4 挑战者并行独立审查(自洽性/完整性+触点完整性/过度工程化/标准对齐),scope=meta,bootstrap 4 维基线全配,M2 §6 模板 runtime 嵌入。
> 预校验:4 份输出的「已对照用户原话」section 全部完整(带 timestamp+原话引录),主线-支线-关系校验全 ✅,无 reject。

## 总结

- 自洽性(挑战者 1):🔴 4 个 / 🟡 6 个 / 🟢 2 个
- 完整性+触点(挑战者 2):🔴 3 个 / 🟡 9 个
- 过度工程化/简洁性(挑战者 3):🟡 2 个 / 🟢 3 个(另附 8 件大机制"必要复杂度白名单"留痕)
- 标准对齐(挑战者 4):🔴 1 个 / 🟡 3 个(引用抽查 25+ 处全部属实)

## 共识问题(多挑战者独立发现,严重性按规则升级)

1. **D11「偏好文件不分发下游」未回灌设计正文**——4/4 挑战者独立发现(最强共识)。正文 6 处仍按分发撰写(I7 :235 / §8.0 :715 / §8.1 :728+:731 / §8.3 :759 / §4.4 :546 / §6.1 :626),另 2 处 🟡 残留标记(:126/:473)、:470"单项目"定性与决策相反;且修正后产生新空洞:下游九格第 8 格承载未定义。→ 🔴
2. **空暂存常规覆写无合法 promotion 写法(死状态)**——3/4 发现。根源是自检轮加的"弃置计数仍需 ≥1"豁免句:正路(落库当刻)走得越好暂存越空,覆写越无合法出口;与 §4.3.1 skipped 语义(顺延待回收)和"无死状态"自检勾冲突;:423"空话格式被文法本身拒掉"断言为假;§6.1 fixture 漏空暂存用例。→ 🔴
3. **覆写信号生命周期语义不完整**——同区 2 个 🔴 发现:阻塞态声称"不产生覆写信号"但固定序①归档已点燃信号(C1 F2);stale-已核绕闸未声明(残留缺口集合 B1/B2/B6 漏第四处)、清账中途 Stop(用户拍板等待)行为未定义、超 60 分钟窗后果未声明(C2)。→ 🔴
4. **AGENTS.md ×2 共享内核双写无同步义务**——3 挑战者同区发现(同步义务缺/根版无内容骨架/硬规矩全文 vs I6"一行引用"自相矛盾)。→ 🟡 升 🔴
5. **场景 5"发现漂移"挂点虚指 + 降级未标注**——2 发现:"meta-review 文档健康维"不存在(该维属 evaluate,仅 feature 路);§1.2 未标注本批机器件推后的降级。→ 🟡 升 🔴
6. **手工校验命令两套写法 + 空 stdin 契约未定义**——2 发现:`< /dev/null` vs `echo '{}' |`;手工模式下 hook 若走降级 exit 0,场景 4 核账变成恒通过的空操作。→ 🟡 升 🔴
7. **机读契约注释块漏出 §7.2 反问表且反问可答**(D15 机械触发已覆盖同一问题)——2 发现,违反 §1.3"答得出就删"自设判据。→ 🟡 升 🔴

## 必须修复(阻塞进入 planning)

R1-R7 = 上述共识 1-7(修复方向详见各挑战者原文;均为局部修复,无架构性返工)。

## 建议改进(不阻塞,随修订顺手或带入 planning)

- 场景 2 措辞对齐::28"机制强制"与 D7 软扫强度失配,改为"机制承载(同批耦合+软扫+覆写硬核)"或等价(C1 F8)
- §2.3 四层表两处纪律项误填非纪律列,如实标注(C1 F9)
- promotion 锚点/指针路径的前缀基准(自仓库双层)未定义(C2)
- 24h 软提醒三子条件不可实现定义("git 有更新 commit"无基准;"活跃 plan"按现定义自仓库恒真)(C2)
- 80 行砍序不完备(砍尽散文仍超未定义)+ 注入端超限行为未声明(C2)
- check-shelf-registry.sh 规格漏"双层探测"逐件要求(C2)
- §3.3 [待填] 形式闸宣称"SKILL 自查"但 I3 步骤④只有 wc -l(C1 F11.2)
- 下游目录卡"装机即空表"无生产者;setup.sh 既有 5 标准件分发使下游首日即告警,需豁免或自动登记(C1 F7)
- 双层探测样板将复制至 ≥6 份脚本,抽象与否无裁决记录(该模式漂移有实证)(C3 F5)
- D11 精确化①忠实性锚点落文法(条目须"日期+原话引录")+ 路由表偏好行补 A 组 meta-review 后果标注(C2/C4;并入 R1 修)

## 分歧

无实质分歧。挑战者 3 判"弃置≥1"规则为过度收紧(🟡),挑战者 1/2 判其后果为死状态(🔴)——同一问题不同切面,方向一致。

## 盲区(未被覆盖,如实登记)

- 架构分层合规:ARCHITECTURE.md 未定义,按降级口径核(仓库结构约定),未做正式分层审查
- RUBRIC 项目特定标准:未自定义,仅通用基线
- 实战有效性(meta-L4):按设计 §6 推真实使用留痕,本审查不覆盖
- 安全维度:不在 design-review 范围,finishing 时 security-scan 承担

## 正面留痕(防只见问题不见全貌)

- 挑战者 3 的"必要复杂度白名单":晋升门禁/promotion 文法/独立登记 hook/覆写信号 60 分钟窗/AGENTS.md 本体/24h 软提醒/双案兼容/D12-D13 反预留,全部过反向追问,无大件被判过度
- 挑战者 4 零发现维度附证据:核心原则合规(公设/反模式红线/dogfood 边界)、scope 漂移(D1-D16 可溯源)、evidence depth 四档、奖励项体现——均通过
- 4 份报告引用抽查合计 60+ 处,仅 1 处虚指(场景 5 挂点)、0 处引用伪造

## 判定

**不通过——需修复后重新审查**(第 1 次)。全部 🔴 为局部矛盾/缺口修复,无架构性返工;修复后做聚焦重审(修复点+回归)。
