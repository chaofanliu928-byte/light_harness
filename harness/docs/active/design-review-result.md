# 设计审查报告 — codex 接入搁置 batch

> **spec**:`docs/superpowers/specs/2026-05-24-codex-shelved-batch-design.md`
> **review 日期**:2026-05-25(brainstorming + design 阶段 2026-05-24,design-review + 后续 2026-05-25)
> **verdict**:**需修复后重新审查**
> **流程**:扁平 fork 4 挑战者并行(自洽性 / 完整性 / 过度工程化 / RUBRIC 对齐)
> **用户决策(2026-05-25)**:**A 路径 — 维持原决策(11 处 + N=4 + 完整流程),修问题**;不接受挑战者 #3 整体简化建议

---

## 总结

| 挑战者 | 维度 | 🔴 | 🟡 | 🟢 | verdict |
|---|---|---|---|---|---|
| #1 | 自洽性 | **11** | 0 | 0 | 有矛盾 |
| #2 | 完整性 | **6** | 18 | 0 | 有遗漏 |
| #3 | 过度工程化 | 0 | 7 | 2 | "结构性过度"(用户选 A 不接受全盘简化)|
| #4 | RUBRIC 对齐 | **3** | 4 | 2 | 有不合规 |
| **合计**(去重前) | — | **20** | **29** | **4** | 不通过 |

---

## 共识问题(多个挑战者独立发现,高可信度)

| 共识 # | 问题 | 发现者 |
|---|---|---|
| C1 | **"11 处" 口径混乱** — 文件 # / 改动 # / banner 命中 # / commit 文件数 4 种用法在 §1.3 / §6.1 / §8.1 / §8.4 / §9.4 混用 | 挑战者 #1(矛盾 #1, #2, #8)+ 挑战者 #2 |
| C2 | **banner 4 变体 vs "完全统一" 声明矛盾** — §1.6 "完全统一" + §8.2 4 变体 + §6.1 "6/5/4 或 6/5/5 双形态" 三处自相矛盾 | 挑战者 #1(矛盾 #5)+ 挑战者 #3 + 挑战者 #4 |
| C3 | **启动条件泄漏** — §F2/§G3/§G4 banner 字面 "在 codex 接入重启时启用" 跟 §8.2 禁用词 "X 时启用" 同构,但 §6.1 验证 2 grep regex 漏检 | 挑战者 #1(矛盾 #7)+ 挑战者 #4(M) |
| C4 | **C3 场景 P0 vs §6.3 推 P1 验收缺口** — §1.2 C3 标 P0 但 §6.3 把"下游分发兼容性"推 P1 | 挑战者 #2(P0)+ 挑战者 #4 |

---

## 必须修复(阻塞 — 进 planning / M1 前必修)

### Critical 1 — 数字口径统一(共识 C1)
- §1.3 / §6.1 / §8.1 / §8.4 / §9.4 统一"11 处"口径定义:**11 改动 # = 11 处文件改动条目**(其中 model-route 1 文件 含 2 段 banner 算 1 改动 #)
- §6.1 公式 14 → 实际命中数(精确按 banner 字面落地数,允许多形态时给精确范围)
- §6.1 注 "6/5/4 或 6/5/5 双形态" 收敛为单一形态(避免软化措辞一致性)

### Critical 2 — 命名漂移整改(挑战者 #1 矛盾 #5, #6, #11)
- "D 风格 / B 风格 / C 风格" 建立统一约定(决策项 → 选项字母 → 风格名);或全部去掉 "X 风格" 简称改用 "D1 选项 D / D2 选项 B / D3 选项 C" 完整引用
- synthesis-rules.md 段名统一(spec 自承"实际无 §21"— 全 spec 改用 "P2 阶段约束段" 或 "L21 段")
- §6.4 引用悬空(spec 无 §6.4)— 删该引用或新建 §6.4
- D7 与 "过度工程化" 错位关联修正(D7 是 plugin-cc patch,不是 N 维度决策)

### Critical 3 — 启动条件泄漏修复(共识 C3)
- §F2/§G3/§G4 banner 字面 "在 codex 接入重启时启用" → 改写避开 "X 时启用" pattern。建议改为 "若 codex 接入重启,本段约束/检查项激活"(避用"时"字时序触发语义)
- §6.1 验证 2 grep regex 补 "X 时启用 / X 完成后" pattern
- §8.2 禁用词清单跟实际 banner 字面对齐:如允许 "重启时启用",从禁用词清单移出;如不允许,banner 改写

### Critical 4 — "承认 gap" 语义双线(挑战者 #1 矛盾 #4)
- §1.3 不做清单 "不写 §F2 'swap 是潜在方案 + 承认 gap'" 中 "承认 gap" 实际指 **A 风格内容改写假装填补**(disallowed)
- §8.1 改动 #7 §F2 banner 字面 "段头 banner 承认 gap" 中 "承认 gap" 实际指 **B 风格段头标搁置**(allowed)
- 解一致:§1.3 改"不写 §F2 '改写正文为 swap codex 是潜在方案' " — 明确禁止动作是 A 风格内容改写,不是 B 风格段头加 banner

### Critical 5 — 挑战者 #4 子焦点声明 vs 应用不一致(挑战者 #1 矛盾 #3, #9)
- §6.2 L368 标题声明 #4 子焦点 3 项(11 处扩散 + RUBRIC 简洁性 + 启动条件泄漏 grep);§7.1 / §9.6 引用加了第 4 项 "D1-D7 决策都在 §8 体现"
- §9.6 "C 风格 commit 顺序约束" 行不在 §1.6 标记里
- 解一致:§6.2 L368 标题 + 内容 + §7.1 + §9.6 四处声明 #4 子焦点统一为 4 项或 3 项,且 §9.6 表只列 §1.6 标过的维度

### Critical 6 — README 路径差异 vs §6.2 #3 通过判据(挑战者 #1 矛盾 #10 + #2 + #4)
- 改动 #10 用 `harness/docs/...`(根 README 仓库根视角),#11 用 `docs/...`(M4 模板下游视角)
- §6.2 #3 通过判据 "打开能定位",但 §8.6 承认改动 #11 在 harness 仓库根视角打不开
- 解一致:§6.2 #3 通过判据加注 "按各自视角验证 — 改动 #10 在仓库根能打开;改动 #11 在下游 setup 后能打开";或改动 #11 用绝对 spec 路径但加 setup.sh 后处理(YAGNI,选前者)

### Critical 7 — 改动 #1 位置 2 字面缺 "codex"(挑战者 #2 P0)
- spec L545 改动 #1 位置 2 after 字面 `**[2026-05-24] 接入决策搁置...**` 缺 "codex"
- 跟 §6.1 计数 "model-route 2 处含完整字面 `[2026-05-24] codex 接入搁置`" 矛盾
- 修复:L545 banner 字面补 "codex" → `**[2026-05-24] codex 接入决策搁置...**`(或对齐 §8.2 主模板字面)

### Critical 8 — C3 P0 vs §6.3 P1 验收缺口(共识 C4)
- §1.2 把 C3 "下游分发感知" 标 P0
- §6.3 测试边界 "不测下游分发兼容性,属 P1 真实项目阶段"
- 解一致:
  - 选项 1:C3 降为 P1(不阻塞本 batch,但 §1.2 需改优先级)
  - 选项 2:§6.3 给 C3 提供 P0 级验收手段(如下游 setup.sh 模拟 + grep 验证)
  - 建议:选项 1(本 batch 是 harness 自仓库改动,真实下游分发本来就是 P1 阶段)

### Critical 9 — §8.6 setup.sh 复制 harness/README 未验证(挑战者 #2 P0 + #4)
- §8.6 自承 "实际待验证 setup.sh 行为"
- 改动 #11 是否真到达下游 unknown — 若 setup.sh 不复制 harness/README.md,改动 #11 对下游用户**真实可见度为 0**,C3 落空
- 修复:**调度者验证 setup.sh 行为**(grep setup.sh 看是否复制 harness/README)→ 结果写入 §8.6;若不复制,改动 #11 改为 README.md(harness 根)的二级 banner 或重新评估改动 #11 的实际作用

### Critical 10 — §0 偏离规则使用不规范(挑战者 #4)
- design-rules.md §0 偏离规则 L40 "只允许记录结构差异(用什么 heading / 用什么编号 / 调整哪节顺序)"
- spec §0 声明 "§2/§3/§4 全节不适用" 不是结构差异,是 "内容判定"
- 修复:§0 改为只记结构差异(如"使用 §0 + §1 + §5-§9 编号,跳过 §2-§4");"§2/§3/§4 不适用" 移到各节正文开头("本节不适用 — meta scope 措辞调整")

### Critical 11 — RUBRIC 嵌入设计但声明不引(挑战者 #4 🔴 + 共识 C2)
- §0 / §1.6 / §7.1 三处声明 "不引 RUBRIC 维度作 design-review 豁免依据"(字面成立)
- §6.2 挑战者 #4 子焦点 "RUBRIC 简洁性" + §9.6 表 + §9.3 D3 decision 实际嵌入 RUBRIC 维度
- 修复:**择一**:
  - 移除 RUBRIC 嵌入(§6.2 #4 子焦点改为 bootstrap 4 维基线 + 决策映射,不引 RUBRIC)
  - 或修订声明承认 "描述性引用 RUBRIC,但不作 design-review 豁免依据"(显式区分"引用 RUBRIC 描述 risk" vs "用 RUBRIC 维度豁免审查")

### Critical 12 — banner "完全统一" 声明 vs 实际 4 变体(挑战者 #4 🔴 + 共识 C2)
- §1.6 L124 "11 处 banner 措辞必须完全统一(日期 + 风格 + 字段顺序)"
- §8.2 给 4 变体(字面字符串各不同)+ §6.1 注 "允许 6/5/4 或 6/5/5"
- 修复:**择一**:
  - 修订声明为 "11 处 banner **核心字段(日期 + '接入搁置'短语 + spec 路径)** 完全统一,长度变体按场景适配"
  - 或收敛 §8.2 模板为 1-2 个变体

### Critical 13 — iterative_progression 软启动条件泄漏(挑战者 #4 🔴 + Critical 3 同源)
- §1.4 / §8.2 banner / §8.1 改动 #7/§G3/§G4 中 "在 codex 接入重启时启用" 跟 §5.3 禁用词 "X 时启用" 同构
- §5.3 / §8.2 禁用词清单跟实际 banner 字面相互矛盾
- 修复:同 Critical 3

---

## 建议改进(不阻塞 planning,implementer 必读)

### 改动 #N 措辞精确性类(挑战者 #2 系列 🟡)
- 改动 #1 banner 与 §8.2 主模板字段顺序差异(N #1):需 §8.1 引用 §8.2 SSoT
- §8.2 变体 4 ROADMAP 与 改动 #6 实际 banner 完整度不对应(N #2)
- 改动 #6 ROADMAP anchor 不精确(实际无 `### 已识别下一步` 三级标题)(N #3):修 anchor 引用为 "`**已识别下一步**` 段后"
- 改动 #9 handoff 位置 1 anchor 模糊(N #4):明确"`下一步` 行后插 1 行"
- 改动 #2 synthesis §21 命名歧义未消除(N #5):与 Critical 2 同
- 改动 #6 ROADMAP "已识别但搁置" 与 "已识别下一步" 段共存(N #6):说明语义边界,或合并
- 改动 #7 §F2 banner 位置错位(N #7):banner 紧贴 §F2 原句"唯一解药",而非 ### 标题正下方
- 改动 #8 decision-trail 段头格式与现有不一致(N #8):去掉 "(P0.9.4 后置 batch)" 副标题
- 改动 #1 位置 2 vs 位置 1 措辞细节不一致("详见" vs "先按...回溯")(N #9):统一动词
- §8.5 顺序约束未明确 revert 时如何处理(N #10):加 "若 Commit 1 需 revert,Commit 2/3 同步 revert"
- §6.1 验证 1 改动 #6 计数说明不清(N #11):明确"banner 行 1 处,列表行 0 处"
- §1.3 兼容性要求未提 setup.sh 复制 harness/README(N #12):合并入 Critical 9
- §6.2 挑战者 #2 通过判据数据来源未指明(N #13):加 "剩余命中预期清单 — retrospective-guide.md / 历史 audit / decision-trail 旧条 / ECC §11 / recommended-tools"
- 改动 #9 handoff 位置 2 "可选" 不明确(N #14):删 "可选" 或明确启用条件
- §8.4 hook 检 covers 字段格式未指定(N #15):加注 "covers 字段写不含 `harness/` 前缀的路径(hook cwd=harness/,M2 §7.3 sentinel 协议)"
- harness/CLAUDE.md 不提 codex 长期维护无机制(N #16):YAGNI 接受不修
- 改动 #1 banner 与 model-route "何时读" 段语义循环(N #17):重写"何时读"为 "进入 codex 接入实施时 — 当前搁置,详 banner"

### 挑战者 #4 RUBRIC 类(🟡 / 🟢)
- D5 / D7 决策展示不并列对比(N #18):D5 / D7 列出二选的具体内容代价
- "标准级"自标判断未与 4 硬条件正面对照(N #19 🟢):§0 加 4 硬条件逐条评估
- §8.2 主模板字符串占位符 `{文件/段}` 未替换(N #20):说明 implementer 替换规则
- D4 完整流程 vs design-rules 默认 design-review 4 维不一致(N #21):说明 design-review 维度选择对齐 M2 §6 而非 design-rules 默认 4 维

### 挑战者 #1 自洽性其他(部分已合并 Critical)
- §1.3 "做什么" 改动 #2 措辞 "措辞改" 与 §8.1 实际"加 banner + 加新句"不一致(N #22):§1.3 改"段头加 banner + 加 conditional 措辞句"
- §F2 段头 banner 与 "在 codex 接入重启时启用" 语义矛盾(已在 Critical 3)
- §6.1 验证 2 grep 不全(已在 Critical 3)
- §0 声明 §2/§3/§4 不适用未与 §9.1 自检兼容(N #23):§9.8 自检表加 "§9 各小节兼容 §0 §2/§3/§4 不适用声明"
- §6.2 #3 通过判据 与 §8.6 setup.sh 矛盾(已在 Critical 9)

---

## 分歧(挑战者间矛盾)

**挑战者 #3 vs 用户决策**:
- 挑战者 #3 整体 verdict "结构性过度",建议简化(改动数 11→3-5 / spec 982→~150 / N=4→N=2 / 跳过 design-review)
- 用户 2026-05-25 选 A "维持原决策" — **不接受 #3 整体建议**
- 但挑战者 #3 局部 finding 若不引入新 🔴 可考虑(如 §8.2 变体收敛 / §9 子节合并)— **本批 implementer 自决**

---

## 盲区

无明显盲区 — 4 挑战者覆盖 self-consistency / 完整性 / 简洁性 / 合规 4 维。挑战者 #1 视角偏 spec 内部,#2 偏 implementer 视角,#3 偏极简主义,#4 偏 RUBRIC + feedback memory 合规。

---

## 判定

**需修复后重新审查**

- Critical 13 项必须修(去重后)
- 🟡 23 项建议修(implementer 自决)
- 挑战者 #3 整体建议不接受(用户 A 决策)

**下一步**:fork 第三轮 designer,基于本 result 修订 spec → 完成后调度者 verify(可选再 fork 自检挑战者)→ pass 后进 task #8 实施。
