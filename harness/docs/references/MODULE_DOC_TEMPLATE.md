# [模块名] 模块

> ⚠️ 此文档由 AI 在开发过程中自动维护。
> 更新时机：Superpowers 子代理修改模块代码时同步更新。
> 方向评估器会检查文档与代码是否一致。

## 职责

[这个模块解决什么问题，在系统中扮演什么角色]

## 架构位置

- 所属层级：[Types / Config / Repository / Service / UI]
- 上游依赖（本模块依赖谁）：[列出]
- 下游依赖（谁依赖本模块）：[列出]

## 对外接口

### [函数/方法名]
```
签名：functionName(param: Type) → ReturnType
用途：[一句话说明]
调用方：[谁在用这个接口]
注意事项：[边界情况、性能考虑等]
```

### [更多接口...]

## 内部设计

[关键的设计决策和实现思路，帮助下一个开发者（人或 AI）理解"为什么这么做"]

## 数据流

[数据怎么流进这个模块、怎么处理、怎么流出]

## 约束和规则

- [本模块特有的约束，例如"所有数据库操作必须在事务内"]
- [性能要求，例如"查询响应 < 200ms"]

## 变更历史

| 时间 | 变更内容 | 关联功能 |
|------|---------|---------|
| [时间] | [做了什么改动] | [关联的 Superpowers plan] |

## 已知问题和技术债

- [当前已知但未处理的问题]
